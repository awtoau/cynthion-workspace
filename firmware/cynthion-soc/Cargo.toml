[package]
name = "cynthion-soc"
version = "0.1.0"
edition = "2021"
license = "BSD-3-Clause"
description = "Firmware for the Cynthion r1.4 VexiiRiscv SoC"

# Current crates, deliberately.
#
# Cynthion's own moondancer pins riscv-rt 0.11 and riscv 0.10, and lunasoc-hal pins
# embedded-hal to =1.0.0-alpha.9 -- a pre-1.0 alpha whose serial traits were REMOVED
# before 1.0 shipped. Following those pins would inherit seven major versions of drift
# and a HAL interface that no longer exists upstream.
#
# No HAL crate at all: the console needs core::fmt::Write, which is in core. That is
# what makes writeln! work, and it is about six lines to implement.
[dependencies]
riscv = "0.16"
riscv-rt = "0.18"

# Where every hardware peripheral is, generated from the SoC's own memory map by
# `scripts/soc_generate_pac.py`. Only `src/target.rs` refers to it, and only in
# its `not(feature = "qemu")` branch -- see the module comment there for why the
# register accessors it also contains are not used.
#
# Not optional and not behind the `qemu` feature. An optional dependency would
# mean the QEMU build compiled a different `target.rs` than the one that ships,
# and this crate is types and constants: nothing it defines is referenced in a
# QEMU build, so nothing it defines reaches the binary.
cynthion-soc-pac = { path = "../cynthion-soc-pac" }

# Build the same shell against QEMU's `-M virt` instead of the FPGA.
#
# Off by default, and it must stay that way: the hardware image is the product, and a
# feature that had to be remembered on every build would eventually be forgotten on one.
# The feature switches src/target_soc.rs for src/target_qemu.rs and the HyperRAM MMIO
# primitives for a RAM array -- nothing else. scripts/soc_test.py drives it, and pairs it
# with memory-qemu.x; building it by hand without that linker script links .text at 0,
# where virt has no memory, and produces an image that traps on the first fetch.
[features]
qemu = []

[profile.release]
# "z", and the reason is now SPEED, which is the opposite of why it was chosen.
#
# It used to read: "this links into the 63 KiB image region of block RAM ... space
# is the scarce quantity and speed is not". Both halves stopped being true when
# `.text` moved to flash. Block RAM went from 8,532 bytes free to 54,856, so space
# is not scarce; and the CPU now fetches every instruction over SPI, so speed is.
#
# MEASURED ON THE BOARD (`./dev.py optlevel`, #167):
#
#     level   .text     ipc   flash seq   flash rnd
#     z       36160   0.302     9.63 MB/s   0.66
#     s       43348   0.171    11.11        0.68
#     3       64576   0.056    11.57        0.65
#
# 79% more code costs 5.4x the IPC. The cause is the I-cache, not the link: 4 KiB,
# direct-mapped, one way, against 36 KB of `.text` -- so code size drives the miss
# rate almost directly, and every miss is a full quad-SPI transaction.
#
# The giveaway is the last two columns. Flash throughput RISES as the code grows,
# 9.63 to 11.57 MB/s, because bigger code fetches longer runs. The link got
# faster and the CPU got five times slower, which is only possible if what
# changed is how often it has to go there.
#
# So "z" is right, decisively, for a reason nobody wrote down when it was chosen.
# Re-measure with `./dev.py optlevel` if the cache geometry ever changes (#110) --
# a bigger I-cache would move this and could move it a long way.
opt-level = "z"
lto = true
codegen-units = 1
debug = true         # kept -- costs nothing in the binary, and .elf is what gets read
